node_modules/
dist/
.next/
.turbo/
coverage/
*.log
.DS_Store
.env
.env.local
.env.*.local
*.tsbuildinfo
.prisma/
next-env.d.ts

# Claude Code — keep .claude/skills/ + settings.json shared, ignore per-machine state
.claude/settings.local.json
.claude/worktrees/
.claude/skills/ui-ux-pro-max/scripts/__pycache__/

# Local cruft — not part of the kit
amp
node-compile-cache/

# Sources d'illustrations. Les PNG bruts (34 Mo, en double à la racine et dans
# « Images de models/ ») ne sont jamais lus par l'application : elle sert les
# .webp de frontend/public/models/, qui en sont la conversion optimisée et
# pèsent 160 Ko au total. Git conserve tout blob à vie, même supprimé plus
# tard — les committer alourdirait chaque clone et chaque déploiement Vercel
# pour toujours, sans rien apporter.
/*.png
/Images  de models/
/Gpt Image 2 Image To Image API Do.txt

